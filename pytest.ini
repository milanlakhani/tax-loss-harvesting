[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    unit: pure unit tests with no database
    parser: PDF parser tests
    integration: database or service integration tests
filterwarnings =
    ignore::DeprecationWarning
